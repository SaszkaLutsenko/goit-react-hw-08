const NotFoundPage = () => {
    return (
      <div>
        <h1>Page Not Found</h1>
        <p>It seems like you've reached a page that doesn't exist. Please check the URL and try again.</p>
      </div>
    );
  };
  
  export default NotFoundPage;